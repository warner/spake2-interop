
# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
style = pep440
versionfile_source = src/spake2_interop_server/_version.py
versionfile_build = spake2_interop_server/_version.py
tag_prefix =
parentdir_prefix = spake2-interop-server
